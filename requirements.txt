# Local dev/test only -- NOT tied to the EMR cluster's Spark version.
# EMR (release emr-7.13.0) runs its own pre-installed Spark 3.5.6 on the
# cluster itself; spark-submit there never touches this requirements.txt,
# so there's nothing to "match" by pinning this to 3.5.6. Pinned to 4.2.0
# because it's the version this was actually developed and tested against;
# older pyspark (<=3.5.x) imports the stdlib `distutils` module, which was
# removed in Python 3.12 and breaks `from pyspark.ml.fpm import FPGrowth`
# outright -- verified locally (ModuleNotFoundError: No module named
# 'distutils'). If you're on Python <3.12, older pyspark may work, but
# there's no reason to change this pin.
pyspark==4.2.0
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
mlxtend==0.25.0
scipy==1.18.0
pyarrow==25.0.0
psutil==7.2.2
joblib==1.5.3
threadpoolctl==3.6.0
