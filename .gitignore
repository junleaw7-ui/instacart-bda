# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Spark
spark-warehouse/
metastore_db/
derby.log

# Data (fetched separately, see data/README.md)
data/*.csv
data/*.parquet
data/*.zip
!data/README.md

# OS
.DS_Store
Thumbs.db

# Office lock files
~$*

# Generated run artifacts (regenerate via the scripts; not committed)
bigdata/output/
nonbigdata/output/
# bigdata/output_emr/ IS committed -- real full-dataset (3.2M orders / 32M
# rows) results from the actual AWS EMR run, not a regenerable local sample.
# benchmark_results.csv IS committed -- it's the performance-comparison data
# behind the written report's Section 5.3, not a regenerable sample-run byproduct.
